{
  "rules": {
    "status": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['state', 'nickname', 'lastChanged'])"
      }
    },
    ".read": false,
    ".write": false
  }
}
